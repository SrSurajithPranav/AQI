streamlit==1.20.0  # Adjust to the latest compatible version
pandas==2.0.0  # Adjust to the latest compatible version
numpy==1.24.4  # Adjust to the latest compatible version
scikit-learn==1.2.2  # Adjust to the latest compatible version
tensorflow==2.14.0  # Adjust to the latest compatible version
